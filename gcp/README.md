# GCP Deployment

Coming soon.

This deployment will use:
- Google Cloud Storage (document upload)
- Cloud Pub/Sub (queue)
- Cloud Run (document processing)
- Vertex AI (embeddings)

Same `VECTOR_STORE` abstraction. Same chunk schema.
