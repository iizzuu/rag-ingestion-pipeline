# Azure Deployment

Coming soon.

This deployment will use:
- Azure Blob Storage (document upload)
- Azure Service Bus (queue)
- Azure Container Instances (document processing)
- Azure OpenAI (embeddings)

Same `VECTOR_STORE` abstraction. Same chunk schema.
